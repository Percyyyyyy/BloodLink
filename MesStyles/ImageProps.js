import { StyleSheet } from "react-native";

const imageProps = StyleSheet.create(
    {
        logoApp: {
            width: "50%",
            height: "100%",
            resizeMode: "contain",
            padding : 20
        },
    }
)

export default imageProps;